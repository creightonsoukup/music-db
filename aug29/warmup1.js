function xo (string) {
  return string.length % 2 === 0
}

console.log(xo("xoxoxox"))

function intersection (arr1,arr2) {
var arr =  arr1.filter(intersection2)
  return arr
}

function intersection2 (arr2) {
  for (var i = 0; i < arr2.length; i++) {
    if (this === arr2[i]) {
      return true
    }
  }
}

console.log(intersection([1,2,4,5],[1,2,3]))
